<UserControl x:Class="SGTC.Views.PrimaryCircuit"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SGTC.Views"
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="800"
             Background="White">
    <Grid x:Name="PrimaryCircuitGrid">
        <Grid.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="Margin" Value="5,2"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>

            <!-- Style for TextBox -->
            <Style TargetType="TextBox">
                <Setter Property="Margin" Value="5,2"/>
                <Setter Property="Padding" Value="5"/>
            </Style>

            <Style TargetType="ComboBox">
                <Setter Property="Margin" Value="5,2"/>
                <Setter Property="Padding" Value="5"/>
            </Style>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>

        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <!-- Column headers -->
        <TextBlock Grid.Column="0" Grid.ColumnSpan="2" HorizontalAlignment="Left" FontSize="16" Padding="0 5" >Primary Circuit</TextBlock>

        <!-- Primary coil rows -->
        <TextBlock Grid.Column="0" Grid.Row="1">Turns</TextBlock>
        <TextBox Grid.Column="1" Grid.Row="1" Text="{Binding PrimaryTurns}" x:Name="PrimaryTurnsInput"/>

        <TextBlock Grid.Column="0" Grid.Row="2">Core Diameter (mm)</TextBlock>
        <TextBox Grid.Column="1" Grid.Row="2" Text="{Binding PrimaryCoreDiameter}" x:Name="PrimaryCoreDiameterInput"/>

        <TextBlock Grid.Column="0" Grid.Row="3">Wire Diameter (mm)</TextBlock>
        <TextBox Grid.Column="1" Grid.Row="3" Text="{Binding PrimaryWireDiameter}" x:Name="PrimaryWireDiameterInput" />

        <TextBlock Grid.Column="0" Grid.Row="4">Wire Insulation Diameter  (mm)</TextBlock>
        <TextBox Grid.Column="1" Grid.Row="4" Text="{Binding PrimaryWireInsulationDiameter}" x:Name="PrimaryWireInsulationDiameterInput"/>

        <TextBlock Grid.Column="0" Grid.Row="5">Wire Spacing (mm)</TextBlock>
        <TextBox Grid.Column="1" Grid.Row="5" Text="{Binding PrimaryWireSpacing}" x:Name="PrimaryWireSpacingInput"/>

        <TextBlock Grid.Column="0" Grid.Row="6">Primary Winding Type</TextBlock>
        <ComboBox Grid.Column="1" Grid.Row="6" x:Name="PrimaryWindingTypeInput" SelectedIndex="0" ItemsSource="{Binding PrimaryWindingTypes}" SelectedItem="{Binding SelectedPrimaryWindingType}">
        </ComboBox>


        <!-- Primary capacitor bank rows-->
        <TextBlock Grid.Column="0" Grid.Row="9">Capacitor Connection</TextBlock>
        <ComboBox Grid.Column="1" Grid.Row="9" x:Name="PrimaryCapacitorConnectionInput" SelectedIndex="0" ItemsSource="{Binding PrimaryCapacitorConnectionTypes}" SelectedItem="{Binding SelectedPrimaryCapacitorConnectionType}">
        </ComboBox>

        <TextBlock Grid.Column="0" Grid.Row="10">Capacitance (nF)</TextBlock>
        <TextBox Grid.Column="1" Grid.Row="10" x:Name="PrimaryCapacitanceInput" Text="{Binding PrimaryCapacitance}"/>

        <TextBlock Grid.Column="0" Grid.Row="11">Capacitor Amount</TextBlock>
        <TextBox Grid.Column="1" Grid.Row="11" Text="{Binding PrimaryCapacitorAmount}" x:Name="PrimaryCapacitorAmountInput"/>
    </Grid>
</UserControl>
